[tool.poetry]
name = "poetry-lock-only-fixture"
version = "0.1.0"
description = "Forter customer case"
authors = ["Yael <yael.bensaya@ox.security>"]

[tool.poetry.dependencies]
python = "^3.10"
requests = "^2.28.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
